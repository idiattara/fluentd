Si IS_ADMIN=TRUE, nous lançons dans la base de données "teggstatus" la requête suivante : "select * from test". Sinon, nous lançons la requête : "select * from toto".

Pour bien formater les données, nous utilisons un processeur Jolt avec la spécification suivante.

Après avoir correctement formaté les données, nous divisons notre JSON de manière à ce que chaque flowfile ne contienne que les métadonnées d'un onglet d'un fichier Excel.
